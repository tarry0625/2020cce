# 2020cce
1.進階題：字串中的數字個數 : 讀入一個至多80個字的字串，找出字串中有多少個數字。
```C
#include <stdio.h>
char c[80];
int z=0;
int main()
{	
	for(int i=0;i<81;i++)
	{
		scanf("%c",&c[i]);
		if(c[i]>='1' && c[i]<='9')
		{
			z++;
		}
		else if(c[i] == '0')
		{
			z++;
		}
	}
	printf("%d",z);
}
```
2.進階題：利用自訂函式最大值max與最小值min求出兩者之差 : 輸入四個正整數後，利用函式判斷最大值與最小值，並輸出最大值減最小值之差
int main(){
  int a,b,c,d;cin>>a>>b>>c>>d;
  cout<<(max(a,b,c,d)-min(a,b,c,d));
  return 0;
}
```C
#include<iostream>
using namespace std;
int max(int a,int b,int c,int d)
{
	if(a>=b && a>=c && a>=d)
	{
		return a;
	}
	else if(b>=a && b>=c && b>=d)
	{
		return b;
	}
	else if(c>=a && c>=b && c>=d)
	{
		return c;
	}
	else if(d>=a && d>=c && d>=b)
	{
		return d;
	}
}
int min(int a,int b,int c,int d)
{
		if(a<=b && a<=c && a<=d)
	{
		return a;
	}
	else if(b<=a && b<=c && b<=d)
	{
		return b;
	}
	else if(c<=a && c<=b && c<=d)
	{
		return c;
	}
	else if(d<=a && d<=c && d<=b)
	{
		return d;
	}
}

int main(){
  int a,b,c,d;cin>>a>>b>>c>>d;
  cout<<(max(a,b,c,d)-min(a,b,c,d));
  return 0;
}
/* 上方C++ main 函式 等同於 下方 C 的 main 函式
int main(void){
  int a, b, c, d;
  scanf("%d %d %d %d", &a, &b, &c, &d);
  printf("%d",  max(a,b,c,d) - min(a,b,c,d) );
  return 0;
}
*/
```
3.進階題：奇數之和 : 輸入一個整數N，輸出由1至N中間的奇數和。
```C
#include <stdio.h>
int main()
{
	int n,s=0;
	scanf("%d",&n);
	for(int i=1;i<=n;i++)
	{
		if(i%2==1)
		{
			s+=i;
		}
	}
	printf("%d",s);
}
```
4.進階題：兩數間可被7整除的數 : 輸入兩個整數，找出兩數之間所有可能被7整除的整數。
```C
#include <stdio.h>

int main()
{
	int a,b;
	scanf("%d %d",&a,&b);
	for(int i=a;i<=b;i++)
	{
		if(i%7==0)
		{
			printf("%d ",i);
		}
	}
}
```
5.基礎題：整數二元四則運算 : 輸入兩個整數 a 與 b 及其 中間的四則運算子 c，輸出其運算結果。
```C
#include <stdio.h>
int main()
{
	int a,b;
	char c;
	scanf("%d %c %d",&a,&c,&b);
	if(c=='+')
	{
		printf("%d",a+b);
	}
	else if(c=='-')
	{
		printf("%d",a-b);
	}
	else if(c=='*')
	{
		printf("%d",a*b);
	}
	else if(c=='/')
	{
		printf("%d",a/b);
	}
}
```
6.基礎題：幾日為星期幾 : 假設某月的1號為星期日，請讀入日數，並找出對應的星期數(週日以0表示，週一以1表示，以此類推，至週六以6表示)。 
```C
#include <stdio.h>
int main()
{
	int n;
	scanf("%d",&n);
	if(n%7==1)
	{
		printf("0");
	}
	else if(n%7==2)
	{
		printf("1");
	}
	else if(n%7==3)
	{
		printf("2");
	}
	else if(n%7==4)
	{
		printf("3");
	}
	else if(n%7==5)
	{
		printf("4");
	}	
	else if(n%7==6)
	{
		printf("5");
	}
	else if(n%7==0)
	{
		printf("6");
	}
	
}
```
7.基礎題：零錢總額 : 假設有50元、5元和1元等3種硬幣，請分別輸入三種硬幣的數量(分別讀入50元、5元、1元硬幣的數量)，計算出這些硬幣的總額。
```C
#include <stdio.h>
int main()
{
	int a50,a5,a1;
	scanf("%d %d %d",&a50,&a5,&a1);
	printf("%d",a50*50+a5*5+a1);
}
```
8.基礎題：兩數平方差 : 讀取兩變數a與b之值，計算出a2-b2之值 
```C
#include <stdio.h>
int main()
{
	int a,b;
	scanf("%d %d",&a,&b);
	printf("%d",a*a-b*b);
}
```
